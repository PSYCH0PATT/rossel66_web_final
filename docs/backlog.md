# Бэклог разнобоя в кабинетах

Что осталось без канона после этапа 5 «канон шапки» (C-01). Здесь **только
фиксация масштаба** — ничего из списка в задачу канона не входило и не чинилось.
Каждый пункт — с примерами файлов и строк, чтобы оценить объём до того, как за
него браться.

Собрано проходом по всем экранам `app/dashboard/admin/**` и
`app/dashboard/artist/**` 24 августа 2026, сразу после применения канона шапки.
Системные причины и волны — в [ui-audit.md](ui-audit.md), вердикты по
иерархии — в [ia-decisions.md](ia-decisions.md).

---

## B-01. `mb-*` на блоках верхнего уровня — теперь мёртвый код

Корень страницы после канона шапки везде `space-y-8`, и он **побеждает**:
селектор Tailwind `.space-y-8 > :not([hidden]) ~ :not([hidden])` специфичнее
`.mb-12` и заодно выставляет этим детям `margin-bottom: 0`. То есть все `mb-*`
на блоках верхнего уровня перестали работать — ритм между секциями стал ровно
32px везде. Классы остались в разметке и вводят в заблуждение: читаешь `mb-12`,
а видишь 32.

Убрать их — правка чисто косметическая (визуально ноль), но затрагивает ~25
мест; в задачу канона не входила. Живыми `mb-*` остаются только у **вложенных**
блоков (не прямых детей корня) — вот там разнобой 32/40/48 настоящий и его
надо разбирать отдельно.

| Значение | Где | Пример |
|---|---|---|
| `mb-12` | сетки статистики, ленты | [admin-dashboard-client.tsx:94](../app/dashboard/admin/dashboard/admin-dashboard-client.tsx#L94), [artist-dashboard-client.tsx:65](../app/dashboard/artist/[username]/dashboard/artist-dashboard-client.tsx#L65), [payments-client.tsx:187](../app/dashboard/artist/[username]/payments/payments-client.tsx#L187), [playlists/page.tsx:82](../app/dashboard/artist/[username]/playlists/page.tsx#L82) |
| `mb-10` | ровно два места | [admin-artists-client.tsx:181](../app/dashboard/admin/artists/admin-artists-client.tsx#L181), [payments-client.tsx:175](../app/dashboard/artist/[username]/payments/payments-client.tsx#L175) |
| `mb-8` | карточки артист-ЛК | [artist-settings-client.tsx:178,199,255](../app/dashboard/artist/[username]/settings/artist-settings-client.tsx#L178), [releases/[id]/page.tsx:143,195](../app/dashboard/artist/[username]/releases/[id]/page.tsx#L143) |
| нет `mb`, ритм от `space-y-8` | весь админ-ЛК | [admin/activity/page.tsx:272](../app/dashboard/admin/activity/page.tsx#L272) |

## B-02. Плотность карточек — семь вариантов паддинга

`card-glass` встречается с семью разными паддингами; правила «когда какой» нет,
на соседних карточках одного экрана значения расходятся.

```
21 × p-6 md:p-8      11 × p-6      8 × p-5      6 × p-4
 4 × p-4 md:p-6       2 × p-0      1 × p-8
```

Примеры расхождения внутри одного экрана: [payments-client.tsx:117](../app/dashboard/artist/[username]/payments/payments-client.tsx#L117) (`p-4 md:p-6`)
против [payments-client.tsx:248](../app/dashboard/artist/[username]/payments/payments-client.tsx#L248) (`p-0`);
[admin/playlists/page.tsx:700](../app/dashboard/admin/playlists/page.tsx#L700) (`p-4 md:p-6`) против
[admin/activity/page.tsx:275](../app/dashboard/admin/activity/page.tsx#L275) (`p-6 md:p-8`).
Просится вариант плотности у самой карточки (`density="compact" | "regular"`),
а не класс по месту.

## B-03. Загрузочное состояние экрана — четыре раскладки

Один и тот же «спиннер посреди страницы» сверстан по-разному, и высота, на
которой он появляется, прыгает от экрана к экрану.

| Раскладка | Файлы |
|---|---|
| `flex justify-center py-16` | [releases/[id]:162](../app/dashboard/admin/releases/[id]/page.tsx#L162), [koala-parser:143](../app/dashboard/admin/releases/koala-parser/page.tsx#L143), [zvonko-parser:176](../app/dashboard/admin/releases/zvonko-parser/page.tsx#L176) |
| `flex items-center justify-center h-64` | [admin/analytics:361,702](../app/dashboard/admin/analytics/page.tsx#L361), [artists/[id]/payments:104](../app/dashboard/admin/artists/[id]/payments/page.tsx#L104), [artists/[id]/playlists:65](../app/dashboard/admin/artists/[id]/playlists/page.tsx#L65), [artists/[id]/releases:64](../app/dashboard/admin/artists/[id]/releases/page.tsx#L64), [artist/analytics:205,345](../app/dashboard/artist/[username]/analytics/page.tsx#L205) |
| `flex min-h-[40vh] items-center justify-center` | [artist releases/[id]:106](../app/dashboard/artist/[username]/releases/[id]/page.tsx#L106) |
| скелетон вместо спиннера | [admin/playlists/loading.tsx](../app/dashboard/admin/playlists/loading.tsx) — единственный `loading.tsx` во всём кабинете |

Просится `PageLoading` в ките (C-14 закрыл `Spinner`, но не его обрамление).
Заодно: скелетон есть ровно у одного роута из 31 — остальные показывают спиннер
или пустой экран.

## B-04. Пустые состояния и экраны ошибок — два поколения

`EmptyState` используется в 31 месте, но ветки «не найдено» на пяти страницах
сверстаны руками, до кита, и с ручной крошкой-ссылкой вместо шапки:

- [admin/artists/[id]/page.tsx:303–315](../app/dashboard/admin/artists/[id]/page.tsx#L303) — `space-y-6` + `<Link>` «Назад к списку артистов» + `Banner`
- [admin/artists/[id]/payments/page.tsx:113](../app/dashboard/admin/artists/[id]/payments/page.tsx#L113) — то же
- [admin/artists/[id]/playlists/page.tsx:74](../app/dashboard/admin/artists/[id]/playlists/page.tsx#L74) — то же
- [admin/artists/[id]/releases/page.tsx:73](../app/dashboard/admin/artists/[id]/releases/page.tsx#L73) — то же
- [admin/artists/[id]/reports/page.tsx:10](../app/dashboard/admin/artists/[id]/reports/page.tsx#L10) — `<div className="text-center py-8 text-gray-400">Артист не найден</div>`, вообще без шапки и действия

У этих экранов нет PageHeader — то есть при ошибке пользователь теряет заголовок
и навигацию. Канон шапки их не трогал: там нет H1.

## B-05. Подпись возврата — три формулировки

Канон C-01 свёл возврат в слот `backHref` шапки, но текст крошки остался разный:

- `К списку` (дефолт компонента) — [artists/add](../app/dashboard/admin/artists/add/page.tsx#L155), [artists/bulk-add](../app/dashboard/admin/artists/bulk-add/page.tsx#L169), [artists/[id]](../app/dashboard/admin/artists/[id]/page.tsx#L324), [releases/add](../app/dashboard/admin/releases/add/page.tsx#L188), [releases/[id]](../app/dashboard/admin/releases/[id]/page.tsx#L212)
- `Назад к списку артистов` — [artists/[id]/payments:134](../app/dashboard/admin/artists/[id]/payments/page.tsx#L134), [artists/[id]/releases:94](../app/dashboard/admin/artists/[id]/releases/page.tsx#L94)
- `Назад` — [artist releases/[id]](../app/dashboard/artist/[username]/releases/[id]/page.tsx#L134)

Плюс внутри контента живут ещё две формулировки того же действия: «Вернуться к
релизам» ([artist releases/[id]:121](../app/dashboard/artist/[username]/releases/[id]/page.tsx#L121))
и «Вернуться к релизу» в состоянии «Релиз удалён»
([admin releases/[id]:181](../app/dashboard/admin/releases/[id]/page.tsx#L181) — судя по контексту, опечатка: ведёт на список).
Тексты в задачу канона не входили, поэтому оставлены как есть.

## B-06. Отступ у SectionHeader — четыре значения

`mb-6` (12 раз), `mb-2` (2), `mb-1` (2), `mb-0` (2) — задаются классом по месту,
хотя это тот же случай, что и с шапкой: отступ должен жить в компоненте.
Примеры: [admin/activity:276](../app/dashboard/admin/activity/page.tsx#L276) (`mb-0`),
[playlists/history:173](../app/dashboard/admin/playlists/history/page.tsx#L173) (`mb-6`),
[artist-settings-client:181](../app/dashboard/artist/[username]/settings/artist-settings-client.tsx#L181) (`mb-6`).

## B-07. Сетки карточек — 14 разных наборов колонок

`grid grid-cols-*` в кабинетах написан 14 несовпадающими способами. Два из них
явно каноничны и повторяются (`grid-cols-2 sm:grid-cols-2 lg:grid-cols-3
xl:grid-cols-4 gap-3 sm:gap-6` — 8 раз; `grid-cols-2 md:grid-cols-2
xl:grid-cols-4 gap-3 md:gap-6` — 2), остальные двенадцать — по одному-два раза.
Разные брейкпоинты дают на планшете разное число колонок у соседних экранов:
[admin-artists-client:181](../app/dashboard/admin/artists/admin-artists-client.tsx#L181)
(`md:grid-cols-3`) против [payments-client:71](../app/dashboard/artist/[username]/payments/payments-client.tsx#L71)
(`md:grid-cols-2 xl:grid-cols-3`).

## B-08. Плотность таблиц — два значения по вертикали

В ячейках `DataTable` живут и `py-3`, и `py-4` (8 и 7 вхождений), при одинаковом
`px-6`. Строки соседних таблиц отличаются по высоте на 8px. Плотность должна
быть пропом `DataTable`, а не классом ячейки.

## B-09. Клампы h1 в `dashboard.css` пережили канон — ЗАКРЫТО (этап 5, часть Г)

Было: `dashboard.css` держал два медиазапроса `.dashboard-theme h1`, которые
перебивали размер H1 на всём, что уже 1024px — `clamp(1.5rem, 7.5vw, 2.25rem)`
до 640 и `clamp(1.75rem, 4.5vw, 3rem)` на 641–1023. Писались они, пока размер
задавался классами в ~30 местах, и после канона шапки означали, что источников
размера по-прежнему два, причём второй бьёт по любому `h1` кабинета, а не только
по шапке.

Закрыто переносом адаптива в [page-header.tsx](../components/ui/page-header.tsx).
Ступени и границы сохранены **один в один**:

| Ширина | Что видно | Чем задано теперь |
|---|---|---|
| ≥1024 | 3rem, line-height 1 | `text-5xl` (база строки) |
| 641–1023 | `clamp(1.75rem, 4.5vw, 3rem)`, lh 1.1, `overflow-wrap: anywhere` | `max-[1023px]:*` |
| ≤640 | `clamp(1.5rem, 7.5vw, 2.25rem)`, lh 1.1, `overflow-wrap: anywhere` | `max-[640px]:*` |

Оба порога — произвольные `max-[…]`, а не `sm:`/`lg:`: они идут через один вариант
Tailwind и гарантированно сортируются по убыванию, тогда как смесь именованного и
произвольного брейкпоинта зависела бы от порядка медиазапросов. `text-[length:…]` —
подсказка типа, иначе `clamp()` можно принять за цвет. Мёртвый `text-4xl` из строки
ушёл: ниже 1024 его перебивал глобальный CSS, выше — `md:text-5xl`.

Правил для `h1` не осталось ни в одном CSS проекта. Из типографики в `dashboard.css`
остались двое, и оба к шапке отношения не имеют: `font-weight: 500` у
`.dashboard-theme` (перебивается `font-bold` самого H1) и `.font-display`.

**`.font-display` из `dashboard.css` убирать нельзя** — проверено на этом же прогоне.
Выглядит как дубль утилиты, которую Tailwind генерит из `fontFamily.display`, но на
деле это корректирующее правило: [globals.css:1571](../app/globals.css#L1571) объявляет
`.font-display` со старым литеральным именем семейства (`'Syncopate'`), шрифт подключён
через next/font под сгенерированным именем в `--font-syncopate`, и в бандле правило из
globals.css идёт ПОСЛЕ утилиты. Без строки в `dashboard.css` дисплейный шрифт на всех
экранах кабинета подменялся фолбэком: первый прогон дал 86 расхождений из 117 (0.5–1 %,
одни и те же — начертание H1 и стат-карточек), строка возвращена.

Проверено:
- числовой замер `getComputedStyle` у H1 (размер, интерлиньяж, `overflow-wrap`) на
  ширинах 390/640/641/768/1023/1024/1440, до и после, на трёх экранах — обычная шапка,
  длинный однословный заголовок («Плейлисты») и `titleStyle="entity"` на карте релиза.
  Совпал во всех точках, включая стык 1023/1024. Пиксельный дифф этого не доказывает:
  он снимается на трёх ширинах, а ошибка в границе живёт ровно между ними. Разрыв
  640→641 (36px → 28.85px) не мой — мобильный кламп кончается выше, чем начинается
  планшетный; перенесён как был;
- визуальный прогон обоих кабинетов в трёх вьюпортах — 1440×900, 390×844 и планшетный
  768×1024, добавленный в каталог `scripts/visual-baseline.ts` на время прогона и потом
  убранный: **расхождений ноль, 117 из 117 скринов совпали**. Единственный DIFF
  (`/dashboard/admin/dashboard` @1440, 1.14 %) оказался известным FOUT иконочного
  шрифта — в кадр попали лигатуры-слова вместо иконок, с `pageerror: A network error
  occurred` на запросе шрифта; пересъёмка того же роута на той же сборке дала 0.000 %.

Известное следствие: единственный `h1` кабинета вне PageHeader — экран «Страница не
найдена» ([dashboard-not-found.tsx](../components/dashboard-not-found.tsx)) — теперь тоже
имеет ровно тот размер, который написан в его классе: 32px на любой ширине вместо
29.25px на 390 и 34.56px на 768, которые ему навязывал глобальный кламп. Это и есть цель
пункта, а не регресс; роута 404 в каталоге визуального прогона нет, замер снят отдельно.

## B-10. Подвал страницы рисует не shell — ЗАКРЫТО (этап 5, часть Б)

Было: `<DashboardFooter />` вызывался вручную из 23 файлов, а не из `DashboardShell`, —
на части экранов его просто не было (остаток F-30). Закрыто не переносом в shell, а
удалением: решение владельца 0-д п.1 (docs/ia-decisions.md) — подвала «Система работает»
в кабинете больше нет, компонент `components/dashboard-footer.tsx` удалён.

## B-11. `cn()` может молча съедать классы — ЗАКРЫТО (этап 5, часть Г)

Было: `cn()` (это `twMerge`) выбрасывал `text-balance` у H1, из-за чего фикс F-83
(перенос «РЕДАКТИРОВАНИ / Е» на 390) годами не работал — при том что класс в
исходнике был.

**Причина была записана неверно, и это меняет правило.** Дело не в размере:
`twMerge` 2.1 не знает утилит переноса из Tailwind 3.4 и относит `text-balance`,
`text-pretty`, `text-wrap`, `text-nowrap` к группе **цвета текста**. Съедает их
идущий следом `text-white` — причём даже внутри ОДНОГО аргумента, то есть склейка
строки не спасла бы:

```
twMerge("text-balance", "text-4xl")   → "text-balance text-4xl"   ← не конфликтуют
twMerge("text-balance", "text-white") → "text-white"               ← класс исчез
```

`text-clip` и `text-ellipsis` `twMerge` знает (группа text-overflow) — рядом с цветом
они целы.

Кит проверен не глазами: разбор `components/ui/*.tsx` компилятором TypeScript,
**229 реальных наборов классов из 42 файлов** — аргументы каждого `cn()` (ветки
тернарников и `&&` разворачиваются в отдельные наборы, иначе взаимоисключающие классы
читаются как пропажа) и раскрытая `cva()`: база × полное произведение всех вариантов
плюс подходящие `compoundVariants`. Пропаж две, обе — законные перекрытия варианта
над базой:

| Файл | Пропал | Вытеснил | Почему законно |
|---|---|---|---|
| `button.tsx`, вариант `cta` | `font-medium` | `font-bold` | главный CTA намеренно жирнее базы (C-02) |
| `alert.tsx`, вариант `destructive` | `[&>svg]:text-foreground` | `[&>svg]:text-destructive` | иконка ошибки красная, а не цвета темы |

Молчаливая пропажа во всём ките ровно одна — та самая строка `TITLE_CLASS`, и она
уже вылечена тем, что не идёт через `cn()`.

Сторож — [components/ui/cn-merge.test.ts](../components/ui/cn-merge.test.ts), 12 кейсов
в двух замках: зафиксированные пары «вход → выход `twMerge`» (упадут, когда обновится
tailwind-merge и семантика групп изменится — это повод перепроверить кит, а не молча
проехать регресс) и живой обход кита тем же разбором — любая НОВАЯ пропажа роняет тест,
законные перекрытия перечислены явным списком с объяснением. Проверено, что тест умеет
падать: с возвращённым `cn()` вокруг `TITLE_CLASS` падают оба замка. Гоняется вместе с
`pnpm test` — глоб в `package.json` расширен на `components/ui/*.test.ts`, до этого
брались только `lib/`.

## B-12. Стенд не показывает половину контента двух экранов — ЗАКРЫТО (этап 5, часть Б)

Не разнобой вёрстки, а дыра в данных: `scripts/seed-e2e.ts` заполняет пользователей,
релизы, отчёты и плейлисты, но почти не заполняет **аналитику** и совсем не заполняет
**историю плейлистов**. Следствие — и живой прогон, и визуальные эталоны снимают эти экраны
в состоянии, которого в бою не бывает:

| Экран | Что в сиде | Что не видно |
|---|---|---|
| `/dashboard/admin/analytics` | 5 строк `streamAnalytics`, все одной датой 2026-06-15 | поведение списков треков (в бою 179 строк), top-10 против «всех», разброс по датам и площадкам — график собирается из одной точки |
| `/dashboard/admin/playlists/history` | 0 строк `PlaylistHistory` | вся таблица: экран всегда в `EmptyState`, фильтры проверить нечем |
| `/dashboard/admin/dashboard`, `/activity` | 0 строк `Activity` (сид их чистил) | лента событий — оба экрана снимались с «Событий пока нет» |

Закрыто в этапе 5 (часть Б): в сид добавлен «каталожный» артист — 16 треков × 14 дней ×
4 площадки, платные и бесплатные, — записи истории плейлистов и пять событий журнала.
Даты этих строк **скользящие** (`daysAgo`, от дня сидирования): окно графика в продукте
календарное, «последние 30 дней от now()», и таким остаётся намеренно — считать его от
последней имеющейся строки значило бы прятать сломанный импорт flash (F-18). Сторож —
сюит «B-12: сид попадает в окно графика» в `tests/integration/stream-metric.test.ts`:
падает, если даты снова прибьют к календарю.

Числа сида контрактные (`1500`/`2200`/`2500` в `tests/integration/stream-metric.test.ts`,
`reports-money.test.ts`, `tests/e2e/cabinet-linked.spec.ts`) — расширять их можно только
данными вне групп E2E Main/Linked либо правя тесты вместе с сидом.

Остаточные пустоты стенда больше не теряются: `scripts/visual-baseline.ts` метит такие роуты
как `warn` с причиной `пустой экран: …` и печатает их списком, а правило приёмки «экран,
снятый пустым, не считается проверенным» лежит в `docs/ui-audit.md`, раздел 4. Прогон
24.08.2026 после правок оставляет шесть таких роутов — их данные ещё некому завести:

| Роут | Пустое состояние | Чего нет в сиде |
|---|---|---|
| `/admin/reports`, вид «Без кабинета» | «Нет отчётов без кабинета» | ничейных отчётов |
| `/admin/artists/[id]` | «Привязанных профилей нет» | связки профилей (её ставят тесты, не сид) |
| `/admin/releases/koala-parser` | «Пока нечего показать» | результата запуска парсера |
| `/admin/settings` | «Нет резервных копий» | истории бэкапов |
| `artist/playlists/[id]` | «Нет данных по трекам» | треков у плейлиста |

Пока они пусты, визуальный дифф по этим экранам ничего не доказывает — при следующей волне,
которая их трогает, сначала данные, потом скрины.

Так и сделано в части В (этап 5): она переверстала карту релиза, поэтому строка
`artist/releases/[id]` из таблицы ушла — сид заполнен до правок. У `e2e-rel-main-1` теперь
два трека с ISRC и длительностью, UPC и приглашённый артист (иначе не видно ни сводки
«2 трека · ISRC · длительность», ни человекочитаемой строки артистов, ни бейджа «Доставлен»:
при нуле треков F-14 честно подменяет его на «Нет данных»), а у `e2e-rel-main-2` — статус
«В доставке» без UPC: только на нём видно пояснение «будет присвоен после доставки» (F-93) и
вторая половина F-92 — точка процесса против галочки финала. Контрактные числа групп
E2E Main/Linked не тронуты: добавлены поля существующих релизов, а не новые записи.

---

# Находки финального ревью (25.08.2026)

Второй слой бэклога: то, что всплыло на финальном прогоне `screens/final` при
разборе по скиллу `design-audit`. Критичное из того же ревью починено сразу и
здесь не лежит — оно описано в [final-review.md](final-review.md), раздел 1.
Нумерация с «Б-», чтобы не путать с вёрсточными B-01…B-12 выше.

## Б-13. Иконки кабинета зависят от Google Fonts — ЗАКРЫТО (самохостинг)

Было: `app/layout.tsx:59` подключал Material Symbols ссылкой на
`fonts.googleapis.com` с `display=swap`. Нет доступа к домену — нет ни одной
иконки во всём кабинете; пока шрифт едет, на их месте видны слова лигатур
(«dashboard», «person», «currency_ruble», «logout»). Это B-04 из
[baseline-issues.md](baseline-issues.md), не закрытый ни одной волной. На
финальном прогоне это дважды испортило кадры (`/admin/dashboard`,
`/artist/…/dashboard`).

Закрыто самохостингом. Тот же самый файл (1 126 804 байта, вариативный, оси
`wght 100..700, FILL 0..1` — один в один запрос из прежней ссылки) лежит в
`public/fonts/material-symbols-outlined.woff2` и раздаётся со своего домена;
`@font-face` и базовое правило `.material-symbols-outlined` — в новом
[app/material-symbols.css](../app/material-symbols.css), `<link>` из шапки убран.

Решения, которые стоит знать при следующей правке:

- **Сабсеттинг не делали.** Имена иконок приходят и динамически (сайдбар — по
  подписи пункта, лента активности — по типу события, `AllTracksToggle` — из
  тернарника, `StatCard` — пропами), поэтому список литералов из грепа неполон
  по построению. Весь файл — 1,1 МБ; лендинг за него не платит, потому что
  `@font-face` грузится лениво, только когда на странице есть элемент с этим
  семейством. По той же причине НЕ добавлен `<link rel="preload">`.
- **`display: block` вместо `swap`** — именно `swap` и показывал слова лигатур.
  У иконки нет осмысленного фолбэка, поэтому короткий невидимый период честнее.
- **Имя семейства оставлено прежним**, поэтому обычный `@font-face`, а не
  `next/font/local` (тот даёт хешированное имя): по имени `Material Symbols`
  ищет сторож эталона в `scripts/visual-baseline.ts`.
- **Вторая половина находки, которой в пункте не было:** `app/globals.css:1`
  тянул строкой `@import url('https://fonts.googleapis.com/…family=Mulish…')` —
  лендинг и формы зависели от того же домена. Mulish переведён на `next/font`
  (скачивается на сборке), литерал `'Mulish'` в семи inline-стилях заменён на
  `var(--font-mulish)`.
- Дырка в CSP закрыта: из `next.config.mjs` ушли `fonts.googleapis.com` из
  `style-src` и `fonts.gstatic.com` из `font-src`. Теперь «доступа к Google нет»
  проверяет сам браузер, а не договорённость.

Проверено: [tests/e2e/icon-font.spec.ts](../tests/e2e/icon-font.spec.ts) (`@smoke`,
два сценария) рубит оба гугловских домена на уровне сети ДО загрузки страницы и
затем требует: ноль запросов к ним; `FontFace` семейства «Material Symbols
Outlined» в статусе `loaded`; ширина каждой иконки сайдбара < 32px — глиф это
квадрат 24px, а слово «library_music» тем же кеглем занимает больше сотни.
Тест умеет падать: с убранным `.woff2` даёт «шрифт не доехал со своего домена».
Второй сценарий делает то же для лендинга и Mulish. В визуальном прогоне пропали
четыре `pageerror: A network error occurred` — те самые кадры, из-за которых
появился R-05.

## Б-14. Склонения пришли не везде — ЗАКРЫТО (`pluralize()`)

`plural()` есть и применяется, но квартальная папка отчётов собирает подпись
строкой: [reports-list.tsx:409](../components/reports-list.tsx#L409) —
`` `${total} отчётов` ``. На экране «1 отчётов», «2 отчётов». То же в виде
«Без кабинета»: [unregistered-reports-list.tsx:234](../components/unregistered-reports-list.tsx#L234)
(«Всего: 1 отчётов») и [:253](../components/unregistered-reports-list.tsx#L253).
Закрыто: все три места идут через `pluralize()` из [lib/plural.ts](../lib/plural.ts)
(число и форма одной функцией) — та же тройка форм, что уже стояла рядом в
`Pagination`. На экране «2 отчёта», «1 отчёт».

## Б-15. `placeholder.svg` рисует английское «No Image» — ЗАКРЫТО (графика без текста)

[public/placeholder.svg](../public/placeholder.svg) — серый квадрат с текстом
`No Image`, шрифт Arial, цвет хексом. Он подставляется в 14 местах и виден на
обложках релизов (`/admin/releases/[id]`, карта релиза артиста), карточках
плейлистов и в сетке `/admin/playlists`. В русскоязычном кабинете это
единственная английская надпись, оставшаяся на видном месте.

Закрыто вторым вариантом — текст убран, остался знак «изображения нет». Русская
подпись не годилась: тот же файл подставляется на ЛЕНДИНГЕ под логотипы
партнёров и фото артистов, где «Нет обложки» было бы неправдой. Заодно из SVG
ушла зависимость от шрифта (был Arial). Путь и имя файла прежние — на них
завязан `lib/playlist-cover.ts` и его тест.

## Б-16. Артистская половина решения 0-а — ЗАКРЫТО (один экран «Отчёты и выплаты»)

Было: `/artist/…/reports` и `/artist/…/payments` — два экрана и два пункта
сайдбара про одни деньги; секция «История отчётов» на `/payments` повторяла
`/reports` карточка-в-карточку. Связывающей фразы про пороги не было:
`/payments` объяснял только 3 000 ₽, хелпер «Ознакомился» — только 500 ₽ (F-17).

Закрыто зеркально админскому 0-а, по тому же скелету, что уже собран в админке
(PageHeader → ряд StatCard → контент):

- Экран один — `/dashboard/artist/[username]/reports`, H1 «ОТЧЁТЫ И ВЫПЛАТЫ».
  Сверху денежный блок с бывшего `/payments` (три StatCard и карточка аванса,
  вынесены в [artist-balance-summary.tsx](../components/artist-balance-summary.tsx)),
  ниже — квартальные карточки отчётов как были.
- `/payments` — редирект на объединённый экран, файл роута оставлен ради
  закладок, как у админского `/admin/payments`.
- Навигация артиста: пять пунктов вместо шести — **Главная → Аналитика →
  Релизы → Отчёты и выплаты → Плейлисты**. Слово «выплаты» в подписи оставлено
  намеренно: артист пользовался этим пунктом, и его исчезновение читалось бы
  как «выплаты убрали», а не «переехали». Крошка мобильной шапки приведена к
  H1 (F-51).
- Пороги связаны одной фразой — `moneyThresholdsHint()` в
  [lib/report-acknowledgment.ts](../lib/report-acknowledgment.ts), туда же
  переехал `MIN_PAYOUT_AMOUNT` из `lib/storage.ts` (модуль чистый, его можно
  импортировать в клиент; прежний экспорт из storage сохранён ре-экспортом).
  Канон честного disabled-хелпера не переписан — он остался как есть.
- Ничего функционального не удалено. Единственное, что было только в дубле, —
  статус «Выплачено / Не выплачено» — переехало на карточки отчётов. Из
  баннера «Недостаточно средств» ушло одно предложение с третьим повтором
  «3 000 ₽»; «у вас накоплено … осталось накопить …» на месте.

Проверено: полный прогон артистских роутов до/после (22 кадра). Расхождения
ровно три и все объяснимы — `artist/reports` (6,3 % на 1440 и 8,7 % на 390:
появился денежный блок и бейдж выплаты), пропавший `artist/payments` (роут стал
редиректом и убран из каталога прогона) и 0,09–0,17 % на всех остальных
экранах — это переименованный пункт сайдбара. Плюс `cabinet-advances.spec.ts`
целиком (он держит все `data-testid` денежного блока) и `@smoke` с новым
сценарием редиректа.

## Б-17. Плейсхолдер пустого UPC в админ-таблице — ЗАКРЫТО (`—`)

Было: канон F-93 — одно тире «—» на весь кабинет; в кабинете артиста так и
есть, а `/admin/releases` печатал `--`.

Закрыто. Строк оказалось не одна, а пять и все в одном файле
[admin-releases-client.tsx](../app/dashboard/admin/releases/admin-releases-client.tsx):
кроме UPC то же `--` печатали три фолбэка `formatDate` и колонка даты. Ревьюер
их не увидел только потому, что в сиде даты заполнены; канон один, поэтому
заменены все пять.

## Б-18. Названия треков усечены при свободном месте — ЗАКРЫТО (шире колонка на десктопе)

Было: карточка «Прослушивания по трекам» в обеих аналитиках резала подпись до
«Трек E2EMAIN0001 — …», хотя справа половина карточки пустая. Вердикт 3.5 прямо
просит не усекать при свободном месте.

Замер (1440/768/390, обе роли): подпись просит 165–175px, колонка держала
фиксированные 130px, а дорожка бара справа при этом занимала 275px. Закрыто
ступенью `xl:w-[190px]` у подписи — на 1440 подписи целые (`scrollWidth` равен
ширине бокса), бар остаётся самым широким элементом строки (215px). Ниже xl
колонка прежняя: там свободного места действительно нет, и `title` с полным
названием остаётся страховкой на всех ширинах.

## Б-19. Placeholder поиска обрезан — ЗАКРЫТО (шире поле + короче подпись)

Было: `/admin/releases` — «Поиск по названию или», `/artist/…/releases` —
«Поиск по названию или UP».

Причина оказалась не в потолке `max-w-sm`, а в шапке: колонка заголовка
`PageHeader` меряется по содержимому, и слот `meta` получал ровно столько,
сколько просил самый широкий его потомок — а вклад поля туда шёл от собственной
ширины `<input>` (233px), а не от класса. При ряде шириной 1098px колонка
занимала 472px, и правая половина ряда пустовала — это и видел ревьюер.

Закрыто с двух сторон: колонке заголовка в [page-header.tsx](../components/ui/page-header.tsx)
добавлен `flex-1` (H1 не двигается — сторож `page-header-canon.spec.ts` зелёный),
у поля появилась явная база `sm:flex-[1_1_20rem]` и потолок `sm:max-w-md`,
подпись сокращена до «Название или UPC» (лупа слева и без слова «Поиск» говорит,
что это поиск). Замер после правки: 1440 — поле 448px, тексту нужно 125 из 370
доступных; 390 — 258 доступных. **Остаток:** ровно на 768 поле по-прежнему
даёт 109px под текст и подпись обрезается — там тулбар из трёх контролов
исчерпал 426px колонки, и это уже случай C-08/B-07, а не подписи.

## Б-20. Процент на карточке артиста без подписи — ЗАКРЫТО (подпись + title)

Было: карточка артиста на `/admin/artists` показывала голое «100%» / «0%»
([admin-artists-client.tsx](../app/dashboard/admin/artists/admin-artists-client.tsx))
— без подписи и без `title`.

**Формулировка пункта была неверной, и это меняет правку.** Это НЕ процент
заполненности профиля: `User.percentage` (`prisma/schema.prisma:27`, `Int?`,
0..100) — процент отчислений артисту, на него умножается сумма отчёта
(`lib/report-processing/index.ts:208-212`), поле формы карточки подписано
«Процент», и `lib/artist-report-requirements.ts` числит его обязательным для
отчётов — отсюда и янтарная подсветка, когда он не задан. Подпись «заполнено на
100%» была бы прямой дезинформацией.

Закрыто: `title="Процент отчислений артисту"` — всегда, плюс видимое слово
«Процент» от `xl` и выше. Порог по замеру мета-строки: 192px на 1440 (подпись
влезает, ФИО рядом целое) против 80px на 768, где ФИО подрезано и без неё.

## Б-21. Два импорта из `lucide-react` в кабинете — ЗАКРЫТО

Было: `ChevronsUpDown` в `admin/activity/page.tsx` и `Loader2` в
`admin/analytics/page.tsx` при каноне «одна иконная система — material-symbols».

Закрыто: `Loader2` → `Spinner` из кита, `ChevronsUpDown` → material-символ
`unfold_more`. Импортов lucide в `app/dashboard/**` больше нет ни одного
(проверено грепом). Внутри `components/ui/**` lucide остаётся законным — это
поставка shadcn.

## Б-22. Регистр значений на карточках парсеров (хвост F-64)

В одном ряду `StatCard` соседствуют «НИКОГДА» капсом и «не запускался»
строчными — значение и его регистр задаются по месту, а не компонентом.

## Б-23. Сумма мимо `formatMoney` в виде «Без кабинета» (хвост F-16)

[unregistered-reports-list.tsx:300](../components/unregistered-reports-list.tsx#L300)
печатает `report.totalAmount.toFixed(2)` — на экране «800.00 ₽»: точка вместо
запятой, лишние нули, обычный пробел перед знаком. Соседние экраны идут через
`formatMoney` и дают «800 ₽» с неразрывным пробелом.

## Б-24. Лента артиста не отфильтрована до трёх типов (решение 0-б) — ЗАКРЫТО

**Было.** `getCachedActivitiesForFeed(row.id, "artist", 100)` вызывался без вида,
`ActivityFeed` при перезагрузке тоже не передавал `view`, а `/api/activities` для
артистской сессии вида не ставил вовсе. Артист видел все типы событий своей группы,
включая «Аванс выдан» и «Аванс удалён» — внутреннюю бухгалтерию лейбла (событие
пишется на админа, но с `metadata.artistId` артиста, и по правилу F-04 приезжало
в кабинет).

**Чем закрыто.** Отдельный вид `artist` в
[lib/activity-views.ts](../lib/activity-views.ts) — артистская тройка вердикта:
статусы релизов (`release_status_updated`, `release_added`,
`parser_release_found`, `parser_release_updated`) · плейлисты (`playlist_found`,
`parser_playlist_found`) · отчётность (`report_status_changed`,
`report_received`). Состав согласован с владельцем 25.08.2026: «статусы релизов»
включают появление релиза, «отчётность» — назначение отчёта. Оговорка №5 (само­
стоятельные действия артиста) сюда не распространяется: у артиста ровно три
группы. Админские виды (`main`/`playlists`/`signatures`/`errors`/`all`) не тронуты.

Фильтрация — на сервере, в двух точках входа, а не пропом вызова:
`getCachedActivitiesForFeed` подставляет вид сама при `role === "artist"`
([cached-dashboard.ts](../lib/cached-dashboard.ts), ключ кэша поднят до
`activities-feed-v3` — в v2 лежали ленты, снятые до фильтра), а
[/api/activities](../app/api/activities/route.ts) ставит его на любую ленту
кабинета, так что ни `view=all`, ни `type=advance_issued` из браузера авансов не
достают. Явно вид передают и все три вызова (экран активности, блок «Последняя
активность», клиентский перезапрос `ActivityFeed`); админский дашборд получил
`view="main"` — тот же состав, что и в RSC, просто без рассинхрона при перезапросе.

**Тесты.** `lib/activity-views.test.ts` — состав вида и неприкосновенность
«Главного»; `tests/integration/activity-feed.test.ts` (сюит «Б-24») — «Аванс
выдан» лежит в базе, но с видом не приходит, а статус релиза и плейлист приходят,
плюс контрольная проверка «без вида аванс приходит»;
`tests/e2e/cabinet-advances.spec.ts` — выданный админом аванс не доезжает до
браузера ни через API с `view=all&type=advance_issued`, ни на экран активности.
Проверено на снятом фиксе: e2e падает без него.

## Б-25. Заголовок события всё ещё режется бейджем на 390 (хвост F-78)

Находка F-78 была про две причины сразу: заголовок события съедали **бейдж и
колонка даты**. Дату убрали — на 390 она с контекстом переезжает под описание
([activity-feed.tsx:202](../components/activity-feed.tsx#L202)), — а бейдж
остался на одной строке с заголовком
([activity-feed.tsx:205-215](../components/activity-feed.tsx#L205)). На 390
заголовку остаётся ~150px, и он по-прежнему обрывается на тринадцатом символе:
«Добавлен пле…», «Артист ознак…». Тип события при этом дублируется бейджем
рядом — то есть режется именно то, что и так понятно, но читать строку
неприятно. Просится перенос бейджа под заголовок до `sm`.

## Б-26. Эталон не воспроизводится побайтово между прогонами

Два прогона подряд на одном коде дают `/admin/dashboard` @1440 расхождение
**1.3 %** при пороге 0.5 %. Причин две, обе в инструменте, а не в продукте:

1. **Относительное время не маскируется.** Лента дашборда печатает «53 мин.
   назад» / «только что», и между прогонами текст меняется.
   `scripts/visual-baseline.ts` гасит анимации (`FREEZE_CSS`) и ждёт шрифты, но
   маскировки динамики в нём нет — этот пункт задания чата 3.5 в код не попал
   (в истории файла его не было ни разу). На экранах с абсолютными датами
   (`/admin/activity`) два прогона дают ровно 0.000 %.
2. **ISR-кэш переживает прогон.** `getCachedAdminDashboard` и
   `getCachedActivitiesForFeed` идут через `unstable_cache` с
   `revalidate = 600`, а кэш лежит в `.next/cache/fetch-cache` и не чистится
   между запусками. Эталон 25.08 сначала снялся с данными, оставшимися от e2e
   (30 отчётов вместо 29, чужие события в ленте), и это заметили только по
   диффу. Сейчас эталон переснят после `rm -rf .next/cache/fetch-cache` — это
   и есть рабочий порядок, но он нигде не записан командой.

Пока не сделано: сравнивая волну с эталоном, помните, что на двух дашбордах
«шум» до ~1.5 % — это время, а не вёрстка. Лечится маскировкой динамики в
`FREEZE_CSS` (фиксированный текст для относительного времени) и очисткой
`fetch-cache` в самом скрипте перед стартом сервера.

---

**Итого по вёрстке: 12 расхождений, открытых осталось восемь — B-01…B-08.**
Самый дорогой по объёму — B-02 (~53 карточки, семь плотностей). Самые дешёвые
и заметные — B-03 (4 раскладки, ~11 мест) и B-06 (18 мест). B-01 (~25 мест)
визуально уже ничего не меняет — это чистка мёртвых классов. B-04 — не
косметика: на этих экранах пользователь остаётся без заголовка и навигации.
B-10 и B-12 закрыты в части Б, B-09 и B-11 — в части Г.

**Итого по финальному ревью: четырнадцать пунктов Б-13…Б-26, закрыто десять**
(Б-13…Б-15 и Б-17…Б-21 — полировка одним заходом, Б-16 и Б-24 — отдельными),
**открыто четыре: Б-22, Б-23, Б-25, Б-26.** Б-26 — про инструмент: пока он не закрыт, дифф по двум дашбордам шумит на относительном
времени, и к этому добавился второй источник шума — **пересев базы между
прогонами**. Сид кладёт скользящие даты аналитики и заново раскладывает порядок
артистов, поэтому `pnpm seed:e2e` (в том числе внутри `global-setup` playwright)
между «до» и «после» даёт ложные DIFF на `/admin/analytics` и `/admin/artists`;
два прогона подряд без пересева совпадают побайтово (проверено, 20/20 по 0.000 %).
