# UI-аудит: системные причины и план закрытия

Проект rossel-music, скоуп — админ-ЛК и артист-ЛК. Входные данные: ui-inventory.md (26 вариантов кнопок #1–#26, 7 групп шапок A–G, arbitrary-цвета, raw-элементы), ui-visual-findings.md (88 находок F-01…F-88), screen-tasks.md (раздел «Ответы владельца» — приоритет: админка read-mostly, ядро = analytics/reports/playlists/artists/dashboard; сервисное — demote, не удалять). Режим: ремонт поверх существующего shadcn, не редизайн. Этапы плейбука: 2.1 токены → 2.2 компоненты → 3 ESLint-enforcement → 4 миграция волнами → 5 перестановки по вердиктам lk-cro; багфиксы — отдельный трек вне волн.

Волны этапа 4 (по частотам владельца):
- **Волна 1 — ядро админки:** dashboard, analytics, reports (+generator), playlists, artists (список/add/bulk-add/[id]).
- **Волна 2 — артист-ЛК целиком:** [username], dashboard, releases (+[id]), analytics, playlists (+[id]), payments, reports, settings, activity + artist-компоненты.
- **Волна 3 — сервисные админ-экраны:** releases (+add/[id]/парсеры), payments, activity, settings, unregistered-reports, playlists/history.
- **Волна 4 — легаси и периферия:** artists/[id]/{payments,releases,playlists}, login, @deprecated-компоненты.

---

## 1. Системные причины (18)

### C-01 «Нет каркаса страницы: PageHeader + shell»
Паттерн «h1 + subtitle + border-b + actions» скопипащен в ~34 экранах в 7 вариантах (3 размера h1, pb-4/6/8, mt-2/mb-2, md/lg); breadcrumb существует в 2 стилях на 5 страницах, в артист-ЛК отсутствует, `generateBreadcrumbs()` в top-nav — мёртвый код; футер есть не везде. Отсюда генерик-H1, рассинхрон крошек и «плавающее» место primary-кнопки.
**Закрывается:** компонент **PageHeader** (props: title, subtitle, backHref/breadcrumbs, actions, size) + ui/breadcrumb внутри; footer — в общий shell/layout; правило «H1 = имя сущности, действие — в subtitle/кнопке»; фикс переноса H1 на 390 (text-balance/размер).
**Закрывает:** F-24 (генерик-H1 «РЕДАКТИРОВАНИЕ»/«РЕЛИЗ»), F-30 (футер не на всех экранах), F-32 (primary то в шапке, то внизу — слот actions), F-51 (крошка ≠ H1), F-83 (перенос «РЕДАКТИРОВАНИ / Е» на 390); инвентаризация: шапки группы A, B, C, D, E, F, G (G/login остаётся автономным, фиксируется как исключение) + мёртвый breadcrumb-код top-nav.
**Этап:** 2.2 (компонент) → 4 (все волны).

### C-02 «48 raw-кнопок в 36 вариантах при живом каноническом ui/button»
Primary-CTA существует в 6 несовместимых написаниях, danger — в 4, серый outline — в 2, ссылки-действия и disabled-состояния — самопал; при этом ui/button уже канонический (6 вариантов, 4 размера, a11y-тач 44px). Причина — кнопки верстают руками, а не берут из кита.
**Закрывается:** довести варианты ui/button (`cta` с glow, `destructive-outline`, success/warning-outline через токены), единый disabled-стиль (реально приглушённый), затем тотальная замена raw `<button>`.
**Закрывает:** F-20 («Обновить» в 3 стилях), F-28 (disabled-CTA выглядит активной), частично F-13/F-25/F-35 (стилевая часть — primary у F-13 в C-03, F-25 в C-14, F-35 в C-04); инвентаризация: кнопки #1,#2 (остаются эталоном), #3,#4,#5,#6 (primary), #7 (серый outline), #8 (danger), #9 (azure→default), #13 (ghost-мини), #20 (success/danger outline), #22 (combobox — остаётся), #23 (link), #24 (outline-pill), #26 (warning-outline).
**Этап:** 2.2 (варианты) → 3 (ESLint: запрет raw `<button>` вне ui) → 4 (все волны).

### C-03 «Иерархия действий не задана: на экране несколько равновесных primary»
Ни один экран не декларирует главное действие: по 2–4 зелёных solid в ряд, деструктив равновесен primary, реальные действия слабее фильтров, «Сохранить» оторван от полей. По ответам владельца большинство действий — сервисные/аварийные и должны уйти в secondary/dropdown.
**Закрывается:** правило «1 primary на экран» из вердиктов lk-cro; сервисные действия (парсеры, синк, CSV, «Привязать релизы», bulk-add) → outline/ghost/overflow-меню; деструктив → destructive-outline + confirm; тумблеры финансовых статусов — с подтверждением.
**Закрывает:** F-09 (несколько равновесных primary; на 390 primary в 3-м ряду), F-13 (деструктив равновесен primary), F-40 (действия слабее фильтров, похожи на крошки), F-43 (60 финансовых тумблеров без подтверждения), F-81 (обложка-экран, «Сохранить» оторван от полей на 390).
**Этап:** 5 (вердикты lk-cro) с опорой на варианты из 2.2.

### C-04 «Цветовые токены расходятся: акцент в 5 написаниях, палитры в коде»
«Изумрудный» живёт как `--primary hsl(160 64% 40%)`, `#10b981` (~44+ хардкодов), `emerald-400/500/600`, config `#00C957`; azure в 2 значениях; ~131 arbitrary-вхождение в tsx + 88 в globals.css + 15 хексов в config; JS-палитры (SOURCE_COLORS, платформенные VK/Yandex/MTS/Sber) захардкожены по месту. Отсюда «одна CTA разного цвета» и стихийные акценты.
**Закрывается:** этап 2.1 — единый `primary`, success/warning/destructive/info-токены, chart-палитра и платформенная палитра как константы; чистка tailwind.config (emerald/azure), чистка дашбордной части globals.css.
**Закрывает:** F-35 (одна CTA синяя/зелёная на страницах-близнецах); инвентаризация: раздел «Arbitrary-цвета и inline-стили» целиком, хекс-части кнопок #4,#5,#6,#9,#14,#19,#26, платформенные цвета playlists, SOURCE_COLORS, `#10b981` ×44.
**Этап:** 2.1 (токены) → 3 (ESLint: запрет hex/arbitrary-цветов в tsx) → 4 (замена по волнам).

### C-05 «Светлые дефолты shadcn: тёмный вид достигается хардкодом»
Дефолтная тема ui/dialog|popover светлая, поэтому фон оверлеев задан 4 разными хексами (#0f0f0f/#141414/#1a1a1a/#0f1117) в 9+ файлах; ui/select форкнут в обход токенов; отдельные элементы (info-баннер, report-processor) остались светлыми внутри тёмного UI.
**Закрывается:** этап 2.1 — честная тёмная тема через css-переменные (background/popover/card/border), дефорк ui/select, перевод «светлых» блоков на токены.
**Закрывает:** F-48 (светлая info-плашка — единственный светлый элемент); инвентаризация: «фон тёмных оверлеев 4 значениями», форк ui/select, светлый report-processor, glass-токен.
**Этап:** 2.1 → 4 (волны 1–3).

### C-06 «Нет компонента Pagination»
Пагинация и счётчики собраны raw-кодом в 3 файлах + ui-версия в reports-list: разная компоновка, разные языки, полный блок при одной странице, счётчик дублируется до 3 раз на экране.
**Закрывается:** компонент **Pagination** (счётчик в одном месте, «На странице», скрытие при ≤1 странице, RU-строки).
**Закрывает:** F-21 (компоновка не унифицирована), F-26 (полная пагинация при 1 странице), F-27 (счётчик ×3 на экране); инвентаризация: кнопки #11, raw-пагинация admin-releases/artists/artist-releases.
**Этап:** 2.2 → 4 (волны 1–3).

### C-07 «Нет SegmentedControl и FilterChip»
Переключатель периода и фильтр-чипы верстаются по месту: активный чип то зелёный CAPS, то синий sentence-case; цвета чипов — inline-hex; в ряду «7Д/30Д/90Д» — «CUSTOM».
**Закрывается:** компоненты **SegmentedControl** и **FilterChip** на токенах, с единым active-состоянием и RU-подписями.
**Закрывает:** F-22 (стиль активного чипа пляшет), языковая часть чипов уходит вместе с C-16 (F-11); инвентаризация: кнопки #12, #19, темизация ui/switch (Switch-цвета inline).
**Этап:** 2.2 → 4 (волны 1–3).

### C-08 «Нет Toolbar: тулбар releases — 15 raw-кнопок с цветами через style»
Уникальная двухрядная шапка admin-releases: 7 кнопок тулбара с `style={{borderColor,color,background}}`, 24 inline style; на 390 заворачивается в 3 ряда, primary уезжает последним.
**Закрывается:** компоненты **Toolbar/ToolbarButton** (+ SearchInput) с мобильным поведением (primary первым, сервисное — в overflow по вердикту C-03).
**Закрывает:** тулбарную часть F-09 (порядок кнопок на 390); инвентаризация: кнопки #10, шапка группы C, SearchInput (raw-поиск releases/artists/profile-filter).
**Этап:** 2.2 → 4 (волна 3, экран releases).

### C-09 «Нет chart-слоя: 3 самописных тултипа и никаких правил осей»
Каждый график сам рисует tooltip (3 несовместимых стиля) и оси: тултип закрывает до половины плота и не закрывается на таче, «переезжает» при скролле, последний тик обрезается, подписи X слипаются на 390, одна шкала глушит 3 серии из 4.
**Закрывается:** компонент **ChartTooltip** (компактный, флип у краёв, закрытие по тапу/скроллу) + общие правила осей (паддинг последнего тика, прореживание тиков на мобиле, нормировка/лог-шкала или toggle серий) + палитра из C-04.
**Закрывает:** F-19 (тултип закрывает плот, desktop+390), F-62 (обрезка «13.08», слипшиеся подписи X), F-79 (одна шкала на 4 серии), F-88 (тултип остаётся при скролле); инвентаризация: chart-tooltip ×3 (streaming-chart, DspStreamChart, Track*Bar).
**Этап:** 2.2 → 4 (волны 1–2).

### C-10 «Таблицы не системные: 10 raw-таблиц с одинаковым thead и без mobile-поведения»
6 админ-таблиц имеют дословно одинаковый thead, ui/Table используется лишь в 3 файлах; ни одна не определяет поведение на узких экранах (обрезка без скролла, скрытые колонки без индикатора) и зону клика строки.
**Закрывается:** пресет **DataTable** на ui/table: обёртка с горизонтальным скроллом + видимым индикатором/тенями, sticky-первая колонка или карточная раскладка на 390, кликабельная строка целиком (одно tap-поведение).
**Закрывает:** F-31 (неочевидная зона клика, двойной тап на 390), F-76 (/payments без горизонтального скролла — тогглы недостижимы), F-77 (скрытые колонки без аффорданса на /releases и /activity); инвентаризация: raw `<table>` ×10, дубль thead ×6, report-preview.
**Этап:** 2.2 → 4 (волны 1–3; F-76/F-77 — первыми в волне 3 как major: их экраны /payments, /releases, /activity — сервисные).

### C-11 «Вложенные скроллы: scroll-trap внутри страницы»
Длинные списки живут внутри карточек с внутренним скроллом без аффорданса: страница перестаёт скроллиться под курсором, на таче — до ~24 экранов контента внутри окна 290px, строки срезаются краем, warning-баннер несёт свой скролл на 67 строк.
**Закрывается:** вердикт lk-cro «top-N строк + ссылка "все"» вместо вложенного скролла; где скролл остаётся — ui/scroll-area с видимым скроллбаром и градиентом-фейдом; баннеры — сворачиваемые (счётчик + разворот).
**Закрывает:** F-38 (строка срезана пополам, скролл без аффорданса), F-39 (scroll-trap списков аналитики, desktop+390), F-44 (баннер на 67 строк первым экраном, скролл-в-скролле).
**Этап:** 5 (перестановки) + 2.2 (scroll-area) → волна 1.

### C-12 «Мобильная навигация: drawer без скролла, в ките нет Sheet»
Бургер-меню — самописный `aside fixed h-full` с overflow visible: нижние пункты (у артиста — «Выйти») недостижимы на 390 в обоих ЛК. В ките отсутствуют sheet/scroll-area.
**Закрывается:** перевод drawer на **ui/sheet + scroll-area** (или min: `overflow-y-auto` + safe-area) в едином sidebar-компоненте.
**Закрывает:** F-75 (drawer не скроллится, logout с телефона невозможен); инвентаризация: sidebar/top-nav дубль (в связке с C-13), отсутствие sheet/scroll-area в ките.
**Этап:** 2.2 → волна 1 (общий shell, чинится один раз для обоих ЛК).

### C-13 «Икон-кнопки и тач-таргеты вне системы»
Икон-кнопки верстаются raw (44px руками, logout в 2 написаниях), появляются icon-only контролы без подписи/действия/тултипа, вкладки-иконки без подписей, кнопка закрытия модалки 16×16 — при том что в ui/button тач-патч 44px уже есть.
**Закрывается:** правило «иконка-кнопка = ui/button size=icon» (тач 44px из коробки) + обязательный aria-label/tooltip или текстовая подпись; вкладки — с подписями; декоративные «кнопки» без действия удаляются.
**Закрывает:** F-66 (икон-кнопка без подписи и действия + серый «дубль»), F-80 (закрытие модалки 16×16), F-84 (4 вкладки-иконки без подписей); инвентаризация: кнопки #15, #17, #18, #21.
**Этап:** 3 (enforcement) → 4 (волны 1–3).

### C-14 «Состояния empty/loading не компонентизированы»
Спиннеры в 3 исполнениях, empty-state — самодельные и нечитаемые, ложный «0» во время загрузки вместо скелетона, в empty-state дублируются CTA двумя разными контролами, обещанный контент просто отсутствует без заглушки.
**Закрывается:** компоненты **EmptyState** (иконка + текст читаемого контраста + ОДНО действие) и **Spinner/Skeleton** (скелетон вместо ложных нулей).
**Закрывает:** F-25 (дубль действия ссылкой и кнопкой в empty-state), F-34 (обещанная таблица без empty-state), F-41 (пустой экран с полным блоком фильтров, empty-state без действия), F-54 (ложный «0 стримов» при загрузке), F-58 (нечитаемый empty-state плейлистов); инвентаризация: спиннеры ×3, empty/loading-состояния (releases-client, streaming-chart).
**Этап:** 2.2 → 4 (все волны).

### C-15 «Нет визуального языка секций, карточек, бейджей и баннеров»
Заголовки секций в 3 стилях, акцент-полосы и иконки секций — случайных цветов и из 3 иконных систем, статус-карточки парсеров с несогласованными декорами и типографикой, один статус в 2 стилях, StatusBadge задублирован на ~100 строк, кнопка закрытия баннера скопирована 3 раза.
**Закрывается:** компоненты **SectionHeader** (один стиль + правило цвета полосы или один цвет), **StatCard** (значение/подпись/акцент по правилу), **StatusBadge** на ui/badge + токены, **Banner/Alert** с onClose; иконки — одна система (material-symbols).
**Закрывает:** F-23 (статус «Не подписан» в 2 стилях), F-57 (заголовки секций в 3 стилях), F-59 (акцент-полосы без логики), F-63 (зелёная плашка-обрезок на карточке), F-64 (регистр/типографика значений), F-65 (одиночный жёлтый акцент без пояснения), F-68 (иконки из 3 систем), F-71 (двойная вложенность заголовков); инвентаризация: кнопки #16 (закрытие баннера ×3), #25 (соцсети-чипы → компонент), StatCard (4+3+admin), ReleaseStatusBadge (дубль ×2 + CSS), SectionHeader (6+ мест), PlatformBadge.
**Этап:** 2.2 → 4 (волны 1–2, парсерные карточки — волна 3).

### C-16 «Нет словаря строк и утилит форматирования»
RU/EN вперемешку (футер, пагинация, «CUSTOM», «No Image», «zvuk»), деньги в разных округлениях и без валюты, два порога без пояснения, склонения («1 отчётов», «2 ТРЕКОВ», «Не указан» для почты), даты без года, одна сущность под 3 названиями, техжаргон («Supabase») в пользовательском тексте.
**Закрывается:** утилиты `formatMoney` (валюта + единое округление/пометка), `formatDate` (dd.mm.yyyy всегда), `plural()`; RU-словарь строк для футера/пагинации/сегментов; глоссарий названий сущностей (один канон на парсер/раздел); копирайт-проход в волнах.
**Закрывает:** F-11 (RU/EN), F-16 (округления и валюта), F-17 (два порога без пояснения связи), F-45 (Supabase в тексте), F-60 (три названия одной сущности), F-61 (склонения/род), F-74 (дата без года).
**Этап:** 2.1 (утилиты/словарь как часть токенов-noop) → 4 (все волны).

### C-17 «Нативные контролы и параллельные системы инпутов»
Нативные date/select/file-инпуты вперемешку с кастомными тёмными контролами на 5 экранах; параллельная система AdminInput/AdminSelect в 2 файлах против ui/input|select в остальных; поля-треки без label (placeholder-only).
**Закрывается:** унификация на ui/input|select, стилизованные date-picker (уже есть в analytics — вынести) и file-input (кнопка «Выбрать файл» из ui/button); правило «у поля всегда label», ликвидация AdminInput/AdminSelect.
**Закрывает:** F-12 (нативные контролы вперемешку), F-82 (placeholder-only поля треков); инвентаризация: raw `<input>` ×14, `<select>` ×3, AdminInput/AdminSelect, styled-jsx на login/analytics.
**Этап:** 2.2 → 4 (волны 1–3).

### C-18 «Layout-шум: пустые зоны сеток и дубли контента»
Двухколоночные сетки собраны без учёта реального контента: пустые зоны до 1.5 экрана на 10 экранах обоих ЛК, карточки с «десктопной» высотой на 390 (50–60% пустоты), полупустые карточки в сетке 2×N, рваная сетка фильтров, секции дублируют уже показанные данные.
**Закрывается:** этап 5 — перестановки по вердиктам lk-cro (одноколоночные формы, стеки вместо принудительных 2 колонок, `h-auto` на мобиле, слияние дублирующих секций); опора на StatCard/SectionHeader из C-15.
**Закрывает:** F-10 (пустые зоны системно, desktop+390), F-53 (техинформация дублирует хиро), F-85 (полупустая карточка артиста + «60%» без подписи).
**Этап:** 5 → применение в волнах 1–2.

---

## 2. Функциональные баги (не UI-система)

Баги данных/логики. Дизайн-системой не закрываются — отдельный багфикс-трек, ВНЕ волн миграции (майнорные можно чинить попутно, но они не зависят от компонентов).

| ID | Суть | Где чинить |
|---|---|---|
| F-01 | bulk-add предзаполнен 22 артистами, включая существующих — 1 клик = 22 дубля | admin/artists/bulk-add: дефолт пустой ввод + серверная дедупликация |
| F-02 | Селект «АРТИСТ» пуст при привязанном артисте — риск затирания связи при «Сохранить» | admin/releases/[id]: инициализация значения селекта из связи релиза |
| F-03 | Записи activity задвоены (Система + сырой числовой ID) одним таймстампом | логика логирования событий: дедуп + резолв ID→имя актора |
| F-04 | Лента активности артиста показывает 1 событие при 4 релизах и 2 отчётах | запрос/фильтр событий артист-activity |
| F-05 | Ось Y графика стримов «0K,0K,0K,0» при 2.8M прослушиваний | форматтер шкалы в admin streaming-chart (округление до K ломается) |
| F-06 | Все 13 обложек Яндекс-секции битые, +2 в МТС | источник/прокси URL обложек playlists + fallback-заглушка |
| F-07 | Битые иконки платформ Яндекс/Spotify в формах артиста | пути ассетов иконок (artists/add, artists/[id]) |
| F-14 | Статус «ДОСТАВЛЕН» при 0 треков и длительности 0:00 | целостность данных релиза / правило вычисления статуса |
| F-15 | Даты отчётов противоречат кварталам (Q4 2025 → 17.08.2026), сортировка по кварталу | поле «дата загрузки» vs «период» в данных отчётов + подпись «Загружен:» |
| F-18 | «60 прослушиваний» на дашборде vs «107 стримов» в аналитике при одном окне | единый источник/период метрики артиста + подпись периода |
| F-33 | Поле «ОБЛОЖКА (URL)» показывает сырой base64 data-URI | хранение обложки (файл/URL вместо base64) либо спец-отображение поля |
| F-37 | Тестовые артисты «test», «cherrypiertd» в боевом списке | чистка данных + сид-политика |
| F-46 | Папки «0 отчётов» с активными «Скачать все»/«Удалить папку» | disable/скрытие действий при пустой папке (логика reports) |
| F-55 | «КИОН Музыка» в легенде без линии и тултипов; линия ВК рвётся на отрезки | построение серий графика артист-analytics (пустые серии — вон из легенды; гэпы данных) |
| F-56 | Из админ-сессии в ЛК артиста сайдбар админский, active-пункт не подсвечен | логика nav: контекст «просмотр как артист» / active-матчер путей |
| F-67 | Дефолт «Дата выдачи» = завтра, а не сегодня | таймзона/вычисление дефолтной даты (artists/[id] Выплаты) |
| F-69 | Строки «0 ₽» числятся невыплаченными и портят счётчик «Невыплаченных 53» | фильтр нулевых сумм в логике payments |
| F-86 | Контент «всплывает» с задержкой, целые пустые вьюпорты при скролле на 390 | lazy-load/пагинация данных (artists, playlists, activity) + скелетоны из C-14 |

**Итого: 18 багов.**

### Статус багфикс-трека

Закрыты (у каждого — тест, воспроизводивший баг до правки):

| ID | Где правило теперь живёт | Тест |
|---|---|---|
| F-03 | `lib/activity-log.ts` — дедуп пар на чтении + подпись актора; парные записи убраны во всех шести местах записи | `lib/activity-log.test.ts`, `tests/integration/activity-feed.test.ts` |
| F-04 | лента кабинета читает по группе профилей и `metadata.artistId` (`lib/storage.ts`, `/api/activities?cabinet=1`) | там же |
| F-05 | `lib/format-compact-number.ts` — одна формула на ось и на метрику | `lib/format-compact-number.test.ts` |
| F-14 | `lib/release-status.ts` — «Доставлен» требует хотя бы один трек, «0:00» → «—» | `lib/release-status.test.ts` |
| F-15 | `lib/report-period.ts` — «Загружен: …» и период из квартала | `lib/report-period.test.ts` |
| F-18 | `lib/stream-window.ts` + `buildCabinetStreamFilters` — одно окно и один источник у дашборда и аналитики | `lib/stream-window.test.ts` |
| F-67 | `lib/business-date.ts` — дефолт даты по МСК, не по часовому поясу браузера | `lib/business-date.test.ts` |
| F-69 | `lib/payments-filter.ts` — нулевые суммы не долг | `lib/payments-filter.test.ts` |

Сняты как неактуальные: F-55 (не воспроизводится у rompy — см. `ia-decisions.md`), F-96 (экран визитки удаляется целиком).

**Два места, за которыми нужен глаз при следующих этапах:**

- **Подпись периода F-18** сейчас стоит в заголовке графика («Всего прослушиваний за 30 дней»), а `ia-decisions.md:402` маршрутизирует её в PageHeader (timestamp в subtitle). Когда дойдёт очередь до C-01 — не продублировать: либо переносить туда, либо снимать пункт с PageHeader.
- **Ось Y у артиста** (немонотонные подписи, `ui-visual-findings.md:60`) на текущем коде не воспроизводится: контракт данных графика — числа и точки по возрастанию даты — закреплён тестом `tests/integration/stream-metric.test.ts`, при его соблюдении recharts таких подписей не даёт. Симптом относим к до-волновой сборке; если всплывёт снова — начинать с этого теста.

Остаётся по F-04 фича-трек: генерация и загрузка отчётов (`/api/reports/*-upload`, `process-python`) не пишут событий вовсе — до этого лента не покажет отчётность, сколько её ни фильтруй.

---

## 3. Локальные UI-фиксы

Не системные и не баги данных — точечные правки, выполняются в своей волне этапа 4.

| ID | Суть | Правка | Волна |
|---|---|---|---|
| F-47 | Три статус-сообщения на экране, два противоречат первому | одно состояние экрана: баннер-warning ИЛИ «всё ок», не оба | 1 (reports) |
| F-70 | Селект сортировки усечён «Дата…» при свободном месте | min-width селекта | 1 (reports) |
| F-87 | Название плейлиста дублируется, белый текст на светлых обложках | убрать текст с обложки, оставить подпись; затемняющий градиент | 1 (playlists) |
| F-29 | Тип события задвоен (бейдж + серая подпись) в одной строке | оставить один бейдж | 2/3 (activity, dashboard-ленты) |
| F-78 | Заголовки событий усечены до ~14 символов бейджами и датой на 390 | двухстрочная раскладка строки события на мобиле | 2/3 (вместе с F-29) |
| F-52 | Хиро «я все еще одна» против «Я всё ещё одна» — дисплейный шрифт теряет регистр и «ё» | не прогонять пользовательские строки через display-шрифт/transform либо шрифт с «Ё» | 2 (артист releases/[id]) |
| F-72 | Кнопка «VK» с двумя иконками вокруг текста | одна иконка (внешняя ссылка) | 2 (профиль артиста) |
| F-73 | Подпись «single» у 2 из 4 строк — композиция прыгает | фиксированный слот версии с «—»/пусто | 2 (артист releases) |
| F-36 | Страницы-близнецы парсеров скомпонованы по-разному | один шаблон парсер-страницы (Zvonko = Koala) | 3 (парсеры) |
| F-42 | Два паттерна подписи тумблера в соседних колонках | один паттерн labeled-switch (значение справа) | 3 (payments) |
| F-49 | Колонки «ТИП» и «ЗАГОЛОВОК» дублируют друг друга | слить в одну колонку | 3 (activity) |
| F-50 | Инпут «Поиск пользователя» + селект «Пользователь» — два контрола одного измерения, рваная сетка | оставить один контрол, выровнять сетку фильтров | 3 (activity) |
| F-08 | Оговорка среды: админ-мобайл проверен на ~500px вместо 390 | процессный пункт: реверификация каждого экрана на честных 390 в чек-листе приёмки волны | все волны (DoD) |

**Итого: 13 локальных фиксов** (12 правок + 1 процессный пункт F-08).

---

## 4. Маршрутизация по этапам

| Этап плейбука | Что закрывается |
|---|---|
| **2.1 Токены** | C-04 (primary/success/warning/info, chart- и платформенная палитры; F-35), C-05 (тёмная тема dialog/popover/select; F-48), C-16-фундамент (formatMoney/formatDate/plural, RU-словарь, глоссарий имён; F-11, F-16, F-17, F-45, F-60, F-61, F-74) |
| **2.2 Компоненты** | C-01 PageHeader (F-24, F-30, F-32, F-51, F-83), C-02 варианты Button (F-20, F-28), C-06 Pagination (F-21, F-26, F-27), C-07 SegmentedControl/FilterChip (F-22), C-08 Toolbar/SearchInput, C-09 ChartTooltip+правила осей (F-19, F-62, F-79, F-88), C-10 DataTable (F-31, F-76, F-77), C-12 Sheet/ScrollArea для drawer (F-75), C-14 EmptyState/Spinner/Skeleton (F-25, F-34, F-41, F-54, F-58), C-15 SectionHeader/StatCard/StatusBadge/Banner (F-23, F-57, F-59, F-63, F-64, F-65, F-68, F-71), C-17 инпуты/date/file (F-12, F-82) |
| **3 Enforcement (ESLint)** | C-02 (запрет raw `<button>`), C-04 (запрет hex/arbitrary-цветов и inline color-style), C-13 (icon-кнопки только через ui/button size=icon + обязательный aria-label; F-66, F-80, F-84), запрет raw `<table>/<input>/<select>` вне ui (C-10, C-17) |
| **Волна 4.1 — ядро админки** (dashboard, analytics, reports+generator, playlists, artists) | Применение C-01…C-17 к ядру; приоритетно: F-19, F-38, F-39, F-44 (analytics), F-75 (общий shell); локальные F-47, F-70, F-87 (F-76/F-77 — не здесь: их экраны в волне 4.3) |
| **Волна 4.2 — артист-ЛК** | Применение системы ко всем экранам артиста (фактически самописный ЛК → перевод на ui); F-19/F-62/F-88 (графики), F-26, F-11-строки, F-23, F-25, F-29, F-58; локальные F-52, F-72, F-73, F-78 |
| **Волна 4.3 — сервисные админ-экраны** (releases+парсеры, payments, activity, settings, unregistered) | C-08 Toolbar (releases), C-10 (F-76 payments, F-77 releases/activity), F-12, F-80, F-81, F-82; локальные F-36, F-42, F-49, F-50 (F-33 — только багфикс-трек: и хранение, и отображение поля зависят от решения по данным) |
| **Волна 4.4 — легаси** | Кнопки #9 (azure), шапки группы D, login (группа G); списание @deprecated |
| **5 Иерархия (вердикты lk-cro)** | C-03 (F-09, F-13, F-40, F-43, F-81 — primary/demote по ответам владельца), C-11 (F-38, F-39, F-44 — top-N вместо вложенных скроллов), C-18 (F-10, F-53, F-85 — перестановка сеток); продуктовые вердикты: объединение payments+reports, чистка ленты активности до 3 типов событий, demote парсеров/синков/CSV/«Привязать релизы»/bulk-add |
| **Багфиксы (вне волн)** | F-01, F-02, F-03, F-04, F-05, F-06, F-07, F-14, F-15, F-18, F-33, F-37, F-46, F-55, F-56, F-67, F-69, F-86 |
| **Процесс/DoD волн** | F-08 — реверификация каждого мигрированного экрана на честных 390 |

---

## 5. Счётчик полноты

**Находок в visual-findings: 88 / распределено: 88.**
- Системные причины C-01…C-18: **57** (C-01: 5, C-02: 2, C-03: 5, C-04: 1, C-05: 1, C-06: 3, C-07: 1, C-08: 0 «своих» F + инвентаризация и часть F-09, C-09: 4, C-10: 3, C-11: 3, C-12: 1, C-13: 3, C-14: 5, C-15: 8, C-16: 7, C-17: 2, C-18: 3)
- Функциональные баги: **18**
- Локальные фиксы (+процессный F-08): **13**
- 57 + 18 + 13 = 88, дублей в первичном распределении нет; перекрёстные упоминания (F-09→C-08, F-11→C-07, F-13/F-25/F-35→C-02, F-86→C-14) — вторичные и на счётчик не влияют.

**Позиций инвентаризации: 33 / распределено: 33** (26 вариантов кнопок + 7 групп шапок).
- Кнопки: #1,#2,#3,#4,#5,#6,#7,#8,#9,#13,#20,#22,#23,#24,#26 → C-02 (хекс-части #4,#5,#6,#9,#14,#19,#26 также C-04); #10 → C-08; #11 → C-06; #12,#19 → C-07; #14 → C-02+C-04(+C-05 glass); #15,#17,#18,#21 → C-13; #16,#25 → C-15.
- Шапки: группы A, B, D, E, F → C-01; группа C → C-01+C-08; группа G (login) → C-01 (фиксируется как автономное исключение, миграция в волне 4.4).
- Прочие блоки инвентаризации (arbitrary-цвета, оверлеи ×4 hex, raw input/select/table, спиннеры, chart-tooltip'ы, StatusBadge, AdminInput, мёртвый breadcrumb-код, globals.css) распределены внутри C-04, C-05, C-09, C-10, C-14, C-15, C-17 — см. поля «Закрывает» каждой причины.
