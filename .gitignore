# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.pnpm-store/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# Значения локального тестового стенда — без секретов, нужны в CI и на любой машине
!.env.e2e

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
chrome_profiles/
__pycache__/
*.pyc
.venv/
*.db
temp_*_config.json

/generated/prisma

# Local / deploy artifacts (не коммитить)
deploy-logs*.txt
# Только каталог бэкапов в корне: неанкоренный "backups/" глушил и app/api/backups/
/backups/
sftp_downloads/*
!sftp_downloads/.gitkeep
*.xlsx
!lib/templates/*.xlsx
# Выписка-фикстура для регрессии отчётов: без неё golden-тест не на чем гонять
!tests/fixtures/*.xlsx
4c1c78d1-*.txt
skills-lock.json

# Agent skills (локально)
.agents/

# data directory (migrated to Supabase)
data/
!data/.gitkeep
data_backup_*/

# SQLite databases (migrated to Supabase)
*.db
*.db-journal
*.db-shm
*.db-wal
.tmp/
.vercel-bypass.secret
.env.vercel.staging.*
.env.e2e.run.local

# Python venv-каталоги парсеров (не было правил вообще: 41 + 56 + 34 МБ)
venv_selenium/
test_env/
parsers/mac_test_env/

# Рантайм-каталоги в корне. Анкерим ведущим слэшем: неанкеренный reports/
# проглотил бы 18 каталогов app/api/reports/*, uploads/ — app/api/uploads/*.
# Та же ошибка, что уже описана выше про backups/.
/tmp/
/logs/
/reports/
/uploads/

# Логи. Раньше покрывались только npm/yarn/pnpm-debug, из-за чего в git попали 6 логов.
*.log

# Дампы парсеров: пишутся и никогда не читаются
parsers/zvonko_release_*.html
parsers/zvonko_release_*_data.json
parsers/zvonko_release_data.json
parsers/zvonko_releases_page.html
parsers/zvonko_releases_analysis.html
parsers/zvonko_pagination_analysis.html
parsers/zvonko_login_page.html
parsers/zvonko_login_analysis.html
parsers/zvonko_first_release.html
parsers/sour_diesel_response_*.html
parsers/zvonko_dom_analysis.json
parsers/zvonko_all_releases_data.json
parsers/comparison_results.json
parsers/koala_output.json
parsers/koala_add_report.json
parsers/add_releases_report.json
parsers/koala_cover_test.txt
parsers/koala_cover_short.txt
parsers/small_test_cover.txt
parsers/test_cover_data_url.txt

# Разовые выводы и офисный экспорт в корне
*.doc
debug_*.html
sftp_analysis_report.json

# Визуальная регрессия (scripts/visual-baseline.ts): 70 PNG на прогон, ~40 МБ.
# Дифф гоняется локально на машине, где снят эталон, — в git скрины не нужны.
# Эталон из 1c0b052 остаётся в истории: её не переписываем, просто больше не растим.
/screens/
