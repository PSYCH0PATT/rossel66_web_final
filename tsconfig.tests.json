{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  },
  "//": "types/**/*.d.ts обязателен: ssh2-sftp-client типизирован ambient-файлом, без него проверка падает на четырёх живых модулях",
  "include": ["types/**/*.d.ts", "tests/**/*.ts", "lib/**/*.ts", "scripts/**/*.ts"],
  "exclude": ["node_modules"]
}
